#include <stdio.h>
#include <math.h>

int main(){

    printf("%f", pow(2, 3)); //this will return 2^3 in decimal

    printf("%f", sqrt(36)); // this will return 6
    printf("%f", ceil(34.3)); //round up
    printf("%f", floor(34.3)); //round down 
    return 0;
}